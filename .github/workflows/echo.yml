name: "printing"

on:
  push:
    branches: [dev-stage]
  workflow_dispatch:

jobs:
  Printing:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: "-"
        run: |
          echo ${{ env.DEVELOPMENT }}
          echo ${{ env.STAGING }}
          echo ${{ env.PRODUCTION }}
